# Copy to .env for LOCAL runs only. .env is git-ignored — never commit it.
# In GitHub Actions these come from repo secrets (gh secret set NAME), not this file.

# Layer 6: the LLM hypothesis step (any Anthropic API key)
ANTHROPIC_API_KEY=

# Optional: pick a cheaper model for the hypothesis step
# TRIAGE_LLM_MODEL=claude-haiku-4-5
