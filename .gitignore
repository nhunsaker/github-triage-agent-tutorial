__pycache__/
*.pyc
.pytest_cache/
node_modules/
dist/
# never commit credentials — keep them in a local .env or a GitHub secret
.env
.env.*
!.env.example
# runtime artifacts from the queue stub and consumer
data/queue/
data/materialized/

# internal build plan, not part of the tutorial
PLAN.md
.DS_Store
.venv/
